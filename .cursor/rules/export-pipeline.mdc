---
description: 洗唛 PDF 导出版面管线规范（DOM→SVG→可编辑文字→PDF），改一块勿坏另一块
globs: src/utils/exportPdf.ts,src/utils/exportText*.ts,src/utils/svgTextToPaths*.ts,src/utils/flattenExportTextSpans.ts,src/utils/textScriptDetect.ts,src/utils/arabicTextExport.ts,src/utils/verifyDigitExport.ts,src/utils/labelTextRender.tsx
alwaysApply: false
---

# 导出管线（仅 live 可编辑文字）

链路：`预览 DOM(.export-capture) → flattenInlineTextSpans → dom-to-svg → 去 textLength → convertSvgTextToLiveText → prepareSvgForEditablePdf → embedWashLabelPdfFonts → patchPdfLiveTextPipeline → svg2pdf → PDF`。

> 所有导出均为 live 可编辑文字模式；已移除旧转曲（outline）管线。

## 字体规范（铁律，不容更改）

| 内容 | 字体 |
|------|------|
| **所有数字（中文稿/翻译区/阿语行内，含小数点）** | **GO.TTF** |
| **所有 `%`** | **FZ.TTF**（中文字体，绝不用 GO） |
| 中文文字、中文标点 | FZ.TTF |
| 英/俄（西里尔）字母 | GO.TTF |
| 阿拉伯字母 | ARIAL.TTF |

> 中文成分行 `36.6%锦纶`：`36.6`→GO，`%`→FZ，`锦纶`→FZ。
> 俄文 `57.7%нитрон`：`57.7`→GO，`%`→FZ，`нитрон`→GO。
> 阿语 `57.7%أكريليك`：`57.7`→GO，`%`→FZ，阿拉伯字母→ARIAL。
> 预览侧拆 label-latin+composition-percent，导出侧按块类型统一处理字体。
> 同步：基础文档 `AGENTS.md` 顶部「字体规范（铁律）」。

## 导出块类型（互不影响）

分类在 `exportTextBlockKind.ts`；prepare 在 `exportTextPrepare.ts`；render 在 `exportTextLiveRender.ts`。
**改某语言/布局只动它那一块的 prepare/render，勿让它走别的块的合并或锚点逻辑。**

| 块 | 内容 | 要点 |
|----|------|------|
| `zh-composition` | `36.6%锦纶` | 合并误拆 tspan；按字体分拆 |
| `ltr-grid-head` | `57.7%` | GO 数字 + FZ % |
| `ltr-grid-body` | `нитрон`/`Acrylic` | 整段 GO |
| `ltr-translated` | inline-wrap 等 | GO + FZ % 或整段 GO |
| `arabic-rtl` | `57.7%أكريليك` | RTL 右锚定位，按字符分字体 |
| `generic-ltr` | 货号等 | 通用 LTR |

## 不可破坏的约束

- **grid 比例列(head)与材质列(body)是两列**：压平时分列压（`flattenLtrGridToken`），写文字时分列写。**禁止**合成 `57.7%нитрон` 一行，否则俄文字距/列对齐与预览不符。
- **阿语 RTL 右锚定位**：navigo 不直接做 bidi/RTL/连写，流程：
  1. `captureExportGeometry`（删 textLength 前）把每行右缘 `tspan.x+textLength` 存进 `data-ex-right`、左缘存 `data-ex-left`
  2. `reshapeArabic`（`arabicShaping.ts`）按上下文映射到 Unicode 呈现形（initial/medial/final + lam-alef）
  3. `toArabicVisualString`（`arabicVisualOrder.ts`，用 `bidi-js`）做 UAX#9 bidi 重排得视觉序
  4. 用 ARIAL 逐字形写文字，按 `[data-ex-left, data-ex-right]` 对齐预览宽度
- **阿语括号镜像**：bidi 浏览器自动翻转括号，导出需注意方向。
- **预览侧 `useLatinDigits = !forExport`**：预览拆 `label-latin`+`composition-percent`，导出不拆。

## 改动后必须

```bash
npm run build
```

新增/调整块类型时，同步更新 `exportTextBlockKind.ts`、对应 prepare/render。
