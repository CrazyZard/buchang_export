---
alwaysApply: false
---
# 青蛙羽绒模板（frog-down）

## 模板信息
- **Template ID**: `frog-down`
- **尺寸**: 30mm × 75mm
- **字体**: 微软雅黑（中英文统一）
- **中文**: 5pt（不自动缩小）
- **翻译**: 4.4pt（min 3.1pt 可缩小）
- **成分标题**: 7pt
- **洗涤维护方式**: 5pt
- **Logo**: `frog.svg`
- **缝头**: 13mm
- **compositionMode**: `down-jacket`

## 批量 Excel 导入
专用 Excel 格式（`parseFrogDownBatchExcel`）：
- 表头：工厂代码 | 款号 | 成份资料 | 充绒量（多列）
- 第1行（成分行）：第0-2列为工厂代码/款号/成分，第3列起为尺码值
- 第2行（克重行）：第0-2列为空，第3列起为对应克重值
- 成分文本：空格分隔 `key:value` 格式（如 `面料:100%聚酯纤维 里料:100%聚酯纤维`）
- `exportFrogDownBatchExcelTemplate()` 下载模板

## 渲染结构
与森马羽绒（`senma-down`）基本一致的 down-jacket 布局：
- Logo → 成分（动态标签，无固定 "面料/挂面" 等）→ 充绒量表格
  - 充绒量标题 "充绒量：(单位：克)" 左对齐，`(单位：克)` 置右
  - 表格 3.8pt，靠左，留边距
- 温馨提示 + 洗涤建议
- 产地 + 款号 + 工厂代码置底
- 翻译侧：Logo → 成分 → 洗涤说明（纯文本）→ 底部信息

## 修改变量
- `isFrogDown` 检查 `template.id === 'frog-down'`
- 与 `isSenmaDown` 并列检查时使用 `||`

## 注意事项
- 不要影响森马羽绒（`senma-down`）和其他模板
- 成分标签动态，根据粘贴文本自动生成 `key:value`
- 清空按钮重置为全空成分状态
